# krkr-web 部署配置 —— 复制为 .env 后修改
# （运行 configure.sh 可交互式生成 .env）

# 对外端口（宿主机访问端口）
PORT=8080

# 数据目录/文件的宿主路径。
# 默认相对 docker-compose.yml 所在目录（即 deploy/ 内）。
# 群晖上建议改成绝对路径，例如 /volume1/krkr/games
GAMES_DIR=./games
COVERS_DIR=./covers
MANIFEST_FILE=./manifest.json
